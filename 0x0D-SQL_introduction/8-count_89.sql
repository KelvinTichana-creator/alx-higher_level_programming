-- This script displays the number of records with id = 89 in the table 'first_table' of the specified database in MySQL server.

-- Select the count of records with id = 89 from the table 'first_table'
SELECT COUNT(*)
FROM first_table
WHERE id = 89;

