-- This script computes the score average of all records in the table 'second_table' from the specified database in MySQL server.

-- Compute the score average
SELECT AVG(score) AS average
FROM second_table;

